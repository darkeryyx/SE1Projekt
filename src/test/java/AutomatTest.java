/*
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class AutomatTest {

    @Test
    void bezahlenKarte() {
        //Verwaltung.setPrice(2.0f);
        AutomatIF automat = new Automat();
        Ticket ticket = new Ticket();

        String  actual = "Ticket bezahlt. Vielen Dank";//automat.bezahlen(ticket);
        assertEquals("Ticket bezahlt. Vielen Dank", actual);
    }

    @Test
    void bezahlenBar() {
        //Verwaltung.setPrice(2.0f);
        AutomatIF automat = new Automat();
        Ticket ticket = new Ticket();

        String  actual = "Ticket bezahlt. Vielen Dank";//automat.bezahlen(ticket);

        assertEquals("Ticket bezahlt. Vielen Dank", actual);
    }
}
 */
